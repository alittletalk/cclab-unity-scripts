using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/*In my third scene, there is a walking sound. I was going to 
control footstep sound with key pressed. But it always not work. 
 I searched some codes online but I do not understand them.How can I do that? I am kind of confused and I think it should be one of my main elements in my 
third scene.*/ 

public class WantToDoButFail : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
